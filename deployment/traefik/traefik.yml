# Traefik Configuration
# Phụ trách: Đàm Tiến Đạt (Phase 6, Task 6.3) + Tống Thanh Phúc
#
# TODO:
# - [ ] Cấu hình entrypoints (HTTP → HTTPS redirect)
# - [ ] DuckDNS DNS challenge cho Let's Encrypt
# - [ ] Router rules cho frontend, backend, mlflow

api:
  dashboard: true

entryPoints:
  web:
    address: ":80"
    # TODO: HTTP → HTTPS redirect
  websecure:
    address: ":443"

# TODO: Certificate resolver cho Let's Encrypt + DuckDNS
# certificatesResolvers:
#   letsencrypt:
#     acme:
#       email: "TODO@email.com"
#       storage: "/etc/traefik/acme.json"
#       dnsChallenge:
#         provider: duckdns

providers:
  docker:
    exposedByDefault: false
