% ============================================================
% PHẦN 4 — CẤU HÌNH HUẤN LUYỆN
% Người phụ trách: mỗi thành viên tự viết cho mô hình của mình
% ============================================================

\section{Cấu hình huấn luyện}
\label{sec:training_config}

\textit{Phần này do từng thành viên tự hoàn thiện theo mô hình mình phụ trách.
Mỗi mô hình cần trình bày đầy đủ các mục con bên dưới.
Xem thêm không gian tìm kiếm hyperparameter của Phase~4 tại Mục~\ref{subsec:tuning_results}.}

% ----------------------------------------
\subsection{Mask R-CNN --- \textit{Lê Xuân Trí}}
\label{subsec:config_maskrcnn}

\subsubsection{Tiền xử lý đầu vào đặc thù}
\textit{[TODO --- Lê Xuân Trí: Mô tả pipeline tiền xử lý riêng của Mask R-CNN (kích thước input, format annotation COCO JSON, chuẩn hóa pixel, v.v.).]}

\subsubsection{Hàm mất mát}
\textit{[TODO --- Lê Xuân Trí: Nêu hàm mất mát sử dụng ($\mathcal{L}_{cls}$, $\mathcal{L}_{box}$, $\mathcal{L}_{mask}$), công thức và lý do.]}

\subsubsection{Thuật toán tối ưu \& Learning Rate}
\textit{[TODO --- Lê Xuân Trí: Ghi rõ optimizer (SGD/Adam/AdamW), learning rate, momentum, weight decay, LR scheduler và lý do lựa chọn.]}

\subsubsection{Siêu tham số}
\textit{[TODO --- Lê Xuân Trí: Liệt kê batch size, số epochs, pretrained weights, image size và các siêu tham số quan trọng khác.]}

% ----------------------------------------
\subsection{YOLO26-seg --- \textit{Nguyễn Hồ Anh Tuấn}}
\label{subsec:config_yolo}

\subsubsection{Tiền xử lý đầu vào đặc thù}
\textit{[TODO --- Nguyễn Hồ Anh Tuấn: Mô tả pipeline tiền xử lý riêng của YOLO26-seg (kích thước $640\times640$, format nhãn YOLO-txt, letterbox, chuẩn hóa pixel, v.v.).]}

\subsubsection{Hàm mất mát}
\textit{[TODO --- Nguyễn Hồ Anh Tuấn: Nêu hàm mất mát ($\mathcal{L}_{box}$, $\mathcal{L}_{cls}$, $\mathcal{L}_{dfl}$, $\mathcal{L}_{mask}$) và lý do.]}

\subsubsection{Thuật toán tối ưu \& Learning Rate}
\textit{[TODO --- Nguyễn Hồ Anh Tuấn: Optimizer, LR, scheduler (Cosine Annealing...) và lý do.]}

\subsubsection{Siêu tham số}
\textit{[TODO --- Nguyễn Hồ Anh Tuấn: Batch size, epochs, variant (nano/small/medium/large), pretrained weights, v.v.]}

% ----------------------------------------
\subsection{RF-DETR --- \textit{Tống Thanh Phúc}}
\label{subsec:config_rfdetr}

\subsubsection{Tiền xử lý đầu vào đặc thù}
\textit{[TODO --- Tống Thanh Phúc: Mô tả pipeline tiền xử lý riêng của RF-DETR (kích thước input, format annotation, chuẩn hóa, v.v.).]}

\subsubsection{Hàm mất mát}
\textit{[TODO --- Tống Thanh Phúc: Nêu hàm mất mát (Hungarian matching loss: CE + GIoU + L1) và lý do.]}

\subsubsection{Thuật toán tối ưu \& Learning Rate}
\textit{[TODO --- Tống Thanh Phúc: Optimizer (AdamW), LR, linear warmup + cosine scheduler và lý do.]}

\subsubsection{Siêu tham số}
\textit{[TODO --- Tống Thanh Phúc: Batch size, epochs, frozen/unfrozen backbone layers, v.v.]}

% ----------------------------------------
\subsection{MobileSAM --- \textit{Đàm Tiến Đạt}}
\label{subsec:config_mobilesam}

\subsubsection{Tiền xử lý đầu vào đặc thù}
\textit{[TODO --- Đàm Tiến Đạt: Mô tả pipeline tiền xử lý riêng của MobileSAM (kích thước $1024\times1024$, image embedding, prompt format, v.v.).]}

\subsubsection{Hàm mất mát}
\textit{[TODO --- Đàm Tiến Đạt: Nêu hàm mất mát (Focal Loss + Dice Loss) với công thức và lý do chọn Focal cho pixel imbalance.]}

\subsubsection{Thuật toán tối ưu \& Learning Rate}
\textit{[TODO --- Đàm Tiến Đạt: Optimizer (Adam), LR, ReduceLROnPlateau và lý do.]}

\subsubsection{Siêu tham số}
\textit{[TODO --- Đàm Tiến Đạt: Batch size, epochs, frozen encoder vs fine-tune toàn bộ, v.v.]}

% ----------------------------------------
\subsection{Mask2Former}
\label{subsec:config_mask2former}

\subsubsection{Tiền xử lý đầu vào đặc thù}
Annotation được giữ ở định dạng \textbf{COCO instance} (\texttt{train/val/test.coco.json}) để khớp với \texttt{Mask2FormerImageProcessor} của Hugging Face. Ảnh được resize về cạnh dài $384$ pixel ở cấu hình baseline và $512$ pixel ở cấu hình tuned, sau đó chuẩn hóa theo mean/std của ImageNet ($\mu = [0{,}485, 0{,}456, 0{,}406]$, $\sigma = [0{,}229, 0{,}224, 0{,}225]$) đúng như phân phối mà trọng số pre-trained trên COCO kỳ vọng. Trong khi huấn luyện, các mask ground-truth được chuyển sang dạng tập mask nhị phân kèm nhãn lớp tương ứng với từng instance.

\subsubsection{Hàm mất mát}
Mask2Former tối ưu hàm mất mát của bài toán mask classification, ghép cặp dự đoán với ground-truth bằng \emph{bipartite matching} (thuật toán Hungarian) trước khi tính loss:
\[
    \mathcal{L} = \lambda_{cls}\,\mathcal{L}_{cls} + \lambda_{ce}\,\mathcal{L}_{ce} + \lambda_{dice}\,\mathcal{L}_{dice},
\]
trong đó $\mathcal{L}_{cls}$ là cross-entropy cho nhãn lớp của mỗi query, còn $\mathcal{L}_{ce}$ (binary cross-entropy theo pixel) và $\mathcal{L}_{dice}$ cùng giám sát chất lượng mask. Nhóm giữ nguyên trọng số mặc định của bản cài đặt gốc ($\lambda_{cls}{=}2$, $\lambda_{ce}{=}5$, $\lambda_{dice}{=}5$). Sự kết hợp BCE và Dice là cần thiết: BCE ổn định gradient ở mức pixel, trong khi Dice trực tiếp tối ưu độ chồng lấp vùng và ít nhạy cảm với mất cân bằng foreground/background --- đặc điểm phổ biến ở ảnh lá nơi vùng bệnh thường nhỏ so với nền.

\subsubsection{Thuật toán tối ưu \& Learning Rate}
Mô hình được huấn luyện bằng \textbf{AdamW}. Cấu hình baseline dùng learning rate $5\times10^{-5}$ với weight decay $1\times10^{-4}$; cấu hình tuned tăng learning rate lên $1\times10^{-4}$ và giảm weight decay xuống $5\times10^{-5}$. AdamW phù hợp với kiến trúc Transformer nhờ tách phần phạt $L_2$ khỏi bước cập nhật momentum, giúp huấn luyện ổn định hơn so với SGD khi fine-tune trên dữ liệu hạn chế.

\subsubsection{Siêu tham số}
Nhóm thử nghiệm hai cấu hình để khảo sát ảnh hưởng của dung lượng backbone, độ phân giải đầu vào và cường độ augmentation (Bảng~\ref{tab:m2f_hparams}). Cả hai đều khởi tạo từ trọng số COCO-instance và huấn luyện $30$ epoch trên Kaggle (GPU P100/T4).

\begin{table}[H]
    \caption{Hai cấu hình huấn luyện Mask2Former (áp dụng cho cả Rice và Coffee)}
    \centering
    \begin{tabular}{lll}
        \toprule
        \textbf{Siêu tham số} & \textbf{run1\_baseline} & \textbf{run2\_tuned} \\
        \midrule
        Backbone        & Swin-T (tiny)       & Swin-S (small)      \\
        Image size      & $384$               & $512$               \\
        Batch size      & $4$                 & $2$                 \\
        Learning rate   & $5\times10^{-5}$    & $1\times10^{-4}$    \\
        Weight decay    & $1\times10^{-4}$    & $5\times10^{-5}$    \\
        Epochs          & $30$                & $30$                \\
        Augmentation    & light               & strong              \\
        \bottomrule
    \end{tabular}
    \label{tab:m2f_hparams}
\end{table}
