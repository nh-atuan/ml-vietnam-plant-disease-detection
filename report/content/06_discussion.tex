% ============================================================
% PHẦN 6 — THẢO LUẬN & PHÂN TÍCH LỖI
% Người phụ trách: mỗi thành viên tự viết cho mô hình của mình;
%                  phần so sánh và lựa chọn best model do cả nhóm.
% ============================================================

\section{Thảo luận và phân tích lỗi}
\label{sec:discussion}

% ----------------------------------------
\subsection{Hiện tượng Overfitting / Underfitting}

\textit{[TODO --- Từng thành viên tự phân tích mô hình của mình dựa vào learning curves (Mục~\ref{sec:results}):
\begin{itemize}
    \item Overfitting: train mAP cao nhưng val mAP thấp hơn đáng kể ($>10\%$); loss val tăng khi loss train tiếp tục giảm.
    \item Underfitting: cả train lẫn val đều thấp; loss không giảm hoặc giảm rất chậm.
    \item Hội tụ tốt: cả hai loss giảm ổn định, gap train--val nhỏ.
\end{itemize}
Ghi rõ cách khắc phục đã áp dụng (Dropout, Early Stopping, tăng augmentation, giảm LR, v.v.).]}

Đối với YOLO26-seg, tập Rice cho thấy xu hướng hội tụ tốt hơn: train loss giảm đều, validation loss không tăng kéo dài ở cuối quá trình huấn luyện, và mask mAP@50:95 tăng ổn định đến khoảng 0.6910 ở epoch 50 trên validation. Khoảng cách train--validation chủ yếu thể hiện qua loss validation cao hơn loss train, nhưng chưa đủ mạnh để kết luận overfitting nghiêm trọng. Tập Coffee có dấu hiệu underfitting hoặc bão hòa sớm theo metric instance mAP, vì mask mAP@50:95 chỉ dao động quanh 0.13 ở cuối quá trình dù learning rate đã giảm dần. Cách xử lý phù hợp cho các lần huấn luyện sau là tăng dữ liệu/augmentation cho các lớp khó, kiểm tra lại chất lượng annotation polygon và thử tăng biến thể mô hình hoặc kéo dài số epoch có early stopping.

% ----------------------------------------
\subsection{Phân tích các trường hợp dự đoán sai}
\label{subsec:error_analysis}

\textit{[TODO --- Từng thành viên: Lấy 2--3 ví dụ cụ thể ảnh mô hình của mình dự đoán sai.
Với mỗi ví dụ: hiển thị ảnh gốc, ground truth mask, predicted mask; đưa ra giả thuyết tại sao sai.]}

\textbf{Lưu ý các cặp lớp dễ nhầm đã được xác định từ EDA (Phase 2):}
\begin{itemize}
    \item \textbf{BrownSpot $\leftrightarrow$ LeafBlast (lúa):} Cả hai có vết nâu, khác nhau ở hình dạng vết và viền màu; Bhattacharyya distance thấp giữa hai lớp.
    \item \textbf{AlgalLeafSpot $\leftrightarrow$ Rust (cà phê):} Cả hai tạo đốm trên lá, màu sắc tương tự trong điều kiện ánh sáng kém.
\end{itemize}

\textit{[TODO --- Từng thành viên: Phân tích xem mô hình của mình có mắc các lỗi nhầm lẫn trên không,
và đưa ra giả thuyết nguyên nhân (nhiễu nền, vết bệnh nhỏ, chồng lấp lá, mất cân bằng lớp...).]}

Với YOLO26-seg, confusion matrix và EDA cho thấy hai nhóm lỗi chính. Trên Rice, các lớp có vết nâu hoặc vùng cháy lá tương tự nhau, đặc biệt BrownSpot và LeafBlast, dễ bị nhầm khi vùng bệnh nhỏ hoặc biên vết bệnh không rõ. Trên Coffee, AlgalLeafSpot và Rust có màu sắc/texture gần nhau trong điều kiện ánh sáng kém, đồng thời vùng bệnh có thể nhỏ và rải rác trên nền lá, làm mô hình phát hiện được vùng tổn thương tổng thể nhưng phân tách instance/lớp chưa tốt theo mAP.

% ----------------------------------------
\subsection{So sánh tổng hợp các mô hình}
\label{subsec:model_comparison}

\textit{[TODO --- Cả nhóm tổng hợp sau khi có đủ kết quả. Điền số liệu vào bảng và xếp hạng các mô hình theo 5 tiêu chí lựa chọn.]}

\begin{table}[H]
    \caption{Bảng so sánh đa tiêu chí giữa 5 mô hình (tổng hợp từ Phase 3)}
    \centering
    \begin{tabularx}{\textwidth}{lXXXXX}
        \toprule
        \textbf{Tiêu chí} & \textbf{Mask R-CNN} & \textbf{YOLO-seg} & \textbf{RF-DETR} & \textbf{MobileSAM} & \textbf{Mask2Former} \\
        \midrule
        mAP@50:95       & --- & --- & --- & --- & --- \\
        mIoU            & --- & --- & --- & --- & --- \\
        Inference (ms)  & --- & --- & --- & --- & --- \\
        Model size (MB) & --- & --- & --- & --- & --- \\
        Train--Val gap  & --- & --- & --- & --- & --- \\
        \midrule
        \textbf{Xếp hạng} & --- & --- & --- & --- & --- \\
        \bottomrule
    \end{tabularx}
    \label{tab:model_comparison}
\end{table}

% ----------------------------------------
\subsection{Mô hình được chọn cho Phase 4}

\textit{[TODO --- Cả nhóm quyết định sau khi hoàn thành bảng so sánh trên.
Điền tên model, dataset (Rice/Coffee) và lý do chọn theo 5 tiêu chí:
mAP@50:95, mIoU, inference time ($< 500$ms/ảnh trên CPU), model size, độ ổn định train/val.]}

\textbf{Quyết định:} Mô hình \textbf{\textit{[TÊN MODEL]}} trên tập \textbf{\textit{[Rice / Coffee]}} được chọn để tinh chỉnh trong Phase 4, vì:
\begin{itemize}
    \item \textit{[TODO: Lý do theo tiêu chí mAP@50:95]}
    \item \textit{[TODO: Lý do theo tiêu chí inference time / khả năng deploy]}
    \item \textit{[TODO: Lý do theo độ ổn định, gap train/val nhỏ]}
\end{itemize}
